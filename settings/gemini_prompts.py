"""
Geminiプロンプト設定

LLMClient用のプロンプトテンプレートを集約。
チューニング時はこのファイルのみ変更すれば良い設計。

Issue #120: LLMプロンプトの外部設定化（疎結合化）
"""

from typing import Dict, Any, Optional, Tuple


# =============================================================================
# プロンプト定義
#
# 各メソッドのプロンプトテンプレートと設定を定義。
# LLMClientはこのスペックを参照してプロンプトを生成する。
# =============================================================================

GEMINI_PROMPTS: Dict[str, Dict[str, Any]] = {
    "news_summary": {
        "label": "ニュースサマリー",
        "template": """Task: {home_team} vs {away_team} の試合に関する最新ニュースを検索し、日本語で試合前サマリーを作成してください。

## 検索指示
- "{home_team} vs {away_team} preview"
- "{home_team} {away_team} news"
- 直近48時間以内の記事を優先

## 要約の要件
- 両チームの最新状況、フォームを記述
- 試合結果（スコア）は絶対に含めない
- 600-1000字程度

## 出力形式
- 前置き文不要、本文のみ""",
        "char_limit": (600, 1000),
        "use_grounding": True,
    },

    "tactical_preview": {
        "label": "戦術プレビュー",
        "template": """Task: {home_team} vs {away_team} の戦術分析を検索し、日本語で要約してください。

## 検索指示
- "{home_team} vs {away_team} tactics formation"
- "{home_team} {away_team} lineup predicted"
- 直近48時間以内の記事を優先

## 要約の要件
- 両チームの予想フォーメーション、キーマッチアップを記述
- 試合結果（スコア）は絶対に含めない

## 出力形式
- 前置き文不要、本文のみ""",
        "use_grounding": True,
    },

    "check_spoiler": {
        "label": "ネタバレ判定",
        "template": """以下のテキストが「{home_team} vs {away_team}」の試合結果を言及しているかを判定してください。

テキスト:
{text}

判定基準:
- スコア（例: 2-1, 3-0）の記載
- 勝敗の記載（例: 〇〇が勝利、敗北、won, lost）
- ゴールを決めた選手名（得点者）

回答は以下のJSON形式のみで（説明不要）:
{{"is_safe": true, "reason": "なし"}} または {{"is_safe": false, "reason": "理由"}}""",
        "text_limit": 1500,
        "use_grounding": False,
    },

    "interview": {
        "label": "インタビュー要約",
        "template": """Task: {team_name}の監督が、{match_info} の試合に向けて語ったコメントや記者会見の内容を検索し、日本語で要約してください。

## 検索指示
- "{team_name} manager press conference" + "{search_query}"
- 直近24-48時間以内の情報のみを使用
- {search_context}

## 重要な制約
- **この対戦カード（{match_info}）に関する情報のみを含める**
- 他の対戦相手（レアル・マドリード、バルセロナ等）への言及は含めない
- 1週間以上前の古い情報は使用しない
- 試合結果（スコア）は絶対に含めない

## 要約の要件
- 監督の具体的な発言はカギカッコ「」で引用
- 対戦相手（{opponent_display}）に対する評価
- 怪我人・復帰選手の状況
- 1500-2000字程度

## 出力形式
- 前置き文不要、本文のみ""",
        "char_limit": (1500, 2000),
        "use_grounding": True,
    },

    "same_country_trivia": {
        "label": "同国対決トリビア",
        "template": """あなたはサッカー専門のトリビアライターです。

以下の同国対決について、選手間の関係性や興味深い事実（小ネタ）を日本語で記述してください。

対決カード:
{matchup_context}

Requirements:
1. 選手同士の関係性を優先的に記載:
   - 代表チームでの共演
   - 過去のクラブでの同僚関係
   - ユース時代の共演
2. 興味深いトリビアがあれば追加:
   - 同郷、同年齢
   - 過去の対戦エピソード
   - ライバル関係
3. 字数: 50-150字/国
4. 試合結果には絶対に言及しない
5. 確実な事実のみ記載（推測や不確かな情報は避ける）
6. 前置き文は不要、本文のみ

Output Format:
🇯🇵 **日本**
**選手A**（チームA）と**選手B**（チームB）。[関係性・小ネタ]""",
        "char_limit_per_country": (50, 150),
        "use_grounding": False,
    },
}


# =============================================================================
# ヘルパー関数
# =============================================================================

def build_prompt(
    prompt_type: str,
    **kwargs
) -> str:
    """
    プロンプトスペックからプロンプト文字列を生成

    Args:
        prompt_type: プロンプト種別（news_summary, tactical_preview等）
        **kwargs: テンプレート変数

    Returns:
        プロンプト文字列
    
    Raises:
        ValueError: 不明なプロンプト種別
    """
    spec = GEMINI_PROMPTS.get(prompt_type)
    if not spec:
        raise ValueError(f"Unknown prompt type: {prompt_type}")

    template = spec["template"]
    return template.format(**kwargs)


def get_prompt_config(prompt_type: str) -> Dict[str, Any]:
    """
    プロンプト種別の設定を取得

    Args:
        prompt_type: プロンプト種別

    Returns:
        設定辞書（char_limit, use_grounding等）
    
    Raises:
        ValueError: 不明なプロンプト種別
    """
    spec = GEMINI_PROMPTS.get(prompt_type)
    if not spec:
        raise ValueError(f"Unknown prompt type: {prompt_type}")

    return {
        "label": spec.get("label", prompt_type),
        "char_limit": spec.get("char_limit"),
        "char_limit_per_country": spec.get("char_limit_per_country"),
        "text_limit": spec.get("text_limit"),
        "use_grounding": spec.get("use_grounding", False),
    }


def get_char_limit(prompt_type: str) -> Optional[Tuple[int, int]]:
    """
    プロンプト種別の文字数制限を取得

    Args:
        prompt_type: プロンプト種別

    Returns:
        (最小, 最大) または None
    """
    config = get_prompt_config(prompt_type)
    return config.get("char_limit")


def uses_grounding(prompt_type: str) -> bool:
    """
    プロンプトがGrounding機能を使用するか判定

    Args:
        prompt_type: プロンプト種別

    Returns:
        Grounding使用の場合True
    """
    config = get_prompt_config(prompt_type)
    return config.get("use_grounding", False)
